# Mirrors the settings in home-manager/nushell.nix's `programs.atuin.settings`
# (Nix machines get their config.toml generated by home-manager instead of this file).
auto_sync = true
workspaces = true
sync_frequency = "1h"
search_mode = "skim"
